import { Typography } from "@mui/material";
import HomeLayout from "../components/layout/HomeLayout";

export default function AnalyticsPage({}) {
  return (
    <HomeLayout currLink={'analytics'}>
      <Typography variant="h4">Analytics</Typography>
    </HomeLayout>
  )
}
