import { Typography } from "@mui/material";
import HomeLayout from "../components/layout/HomeLayout";

export default function DeveloperPage({}) {
  return (
    <HomeLayout currLink={'developer'}>
      <Typography variant="h4">Developer</Typography>
    </HomeLayout>
  )
}
