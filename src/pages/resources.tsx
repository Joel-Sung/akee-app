import { Typography } from "@mui/material";
import HomeLayout from "../components/layout/HomeLayout";

export default function ResourcesPage({}) {
  return (
    <HomeLayout currLink={'resources'}>
      <Typography variant="h4">Resources</Typography>
    </HomeLayout>
  )
}
